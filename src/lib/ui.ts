/**
 * Prefijo para colores blancos con opacidad en línea.
 * Uso: `${tw}0.45)` → `rgba(255,255,255,0.45)`
 *
 * Importar desde aquí en vez de definir `const tw` en cada archivo.
 */
export const tw = 'rgba(255,255,255,'
